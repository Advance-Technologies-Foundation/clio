# Write diagnostic validation

Unit: local invalid-input refusal, native validation failure, successful acknowledgement, transport exception and malformed response; secrets, paths, stacks and hostile text must not appear unfenced/unredacted. Preserve existing JSON fields. Do not claim a failed server response proves no side effect.

MCP: real contract and error-path coverage for changed tools. Disposable lab: invalid temporal value, sequence lifecycle rejection, restricted-object refusal and mixed batch outcomes with independent readback. Verify Ring tests, NativeAOT publish and IPC contracts. No OData-first guidance.
