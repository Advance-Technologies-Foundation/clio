# DataService batch validation

Unit: bounds, malformed operations/UUIDs/types, exact-ID filters, generated query correlation, mixed failures, absent/duplicate/unknown query IDs, root errors, malformed/oversized responses, authorization diagnostics and timeout uncertainty. Prove exactly one non-replayable submission and unchanged single-record tests. Route coverage for Core and Framework.

MCP: discover destructive/non-idempotent classification, reject invalid inputs, invoke the real command in an isolated environment. Live: synthetic records with a failing middle item and successful neighbors, persisted readback, sequence listener side effects without generic lifecycle writes. Record batch size, HTTP write count and elapsed measurement boundary. Ring tests, NativeAOT publish and IPC discovery proof.
