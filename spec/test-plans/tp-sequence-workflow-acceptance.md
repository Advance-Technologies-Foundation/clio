# Validation

Run the manual workflow against the isolated sequence-enabled target with explicitly selected schedule/owner. Require native counts/readback and exactly one current activity per progression. Compare native batch versus single-item calls on the same synthetic Contact and assert identical final values. Record retries separately. Preserve task-level fresh-install/designer proof; do not imply email delivery. Run MCP module tests, real MCP scenarios and Ring compatibility checks when tool descriptions change.
