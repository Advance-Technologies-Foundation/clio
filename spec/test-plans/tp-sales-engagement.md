# Sequence context test plan

TC-U-1: read-only discovery reuses merged schema and DataService; bounded collections.
TC-U-2: absent schema, failed catalog, partial metadata and oversized response fail honestly.
TC-U-3: lookup IDs are environment values, not constants; no operational contact/mailbox reads.
TC-U-4: optional existing sequence uses explicit fields and ID filters; missing prerequisites and truncation visible.
TC-I-1: external MCP discovery/argument validation and environment resolution.
TC-I-2: real sequence-capable environment returns live steps, metadata and configuration with no mutation.
