# Анализ: Требования create-dev-env-4-mac vs Возможности deploy-creatio

**Дата анализа:** 24 ноября 2025  
**Статус:** Проведено детальное сравнение функциональности

---

## 📊 Сводная таблица соответствия

| # | Требование | Статус | Поддержка | Пробелы |
|---|-----------|--------|----------|---------|
| 1.1 | Распаковка ZIP-архива | ✅ Есть | deploy-creatio | - |
| 1.2 | Создание папки `environment` | ⚠️ Частично | deploy-creatio может создавать папку | Не создает именно `environment` |
| 2.1 | `clio create infrastructure` | ❌ Нет | - | **ОТСУТСТВУЕТ** |
| 2.2 | Развертывание K8s сервисов | ❌ Нет | - | **ОТСУТСТВУЕТ** |
| 2.3 | Конфигурация подключения БД | ✅ Есть | deploy-creatio + `clio rdb` | - |
| 3.1 | Редактирование ConnectionString | ✅ Есть | deploy-creatio | - |
| 3.2 | Установка системной настройки Maintainer | ❌ Нет | - | **ОТСУТСТВУЕТ** |
| 3.3 | Изменение CookiesSameSiteMode в config | ❌ Нет | - | **ОТСУТСТВУЕТ** |
| 4.1 | `clio gate` (cliogate установка) | ✅ Есть | deploy-creatio может вызывать | Требует явного вызова |
| 4.2 | `clio cfg` (регистрация окружения) | ✅ Есть | deploy-creatio может вызывать | Требует явного вызова |
| 4.3 | `clio cdp true` (CDP режим) | ⚠️ Частично | Требует явного вызова | Не встроено в deploy-creatio |
| 4.4 | `clio fsm on` (File Design Mode) | ⚠️ Частично | Требует явного вызова | Не встроено в deploy-creatio |
| 5.1 | Интерактивный ввод Environment Name | ✅ Есть | deploy-creatio может запросить | Требует реализации в новой команде |
| 5.2 | Интерактивный ввод Maintainer | ❌ Нет | - | **ОТСУТСТВУЕТ** |
| 5.3 | Параметр --port | ✅ Есть | --SitePort | - |
| 5.4 | Параметры credentials | ✅ Есть | deploy-creatio может поддерживать | Требует добавления параметров |

---

## 🔴 Критические пробелы (ОТСУТСТВУЕТ)

### 1. **Инфраструктура K8s (Фаза 2)**
**Требование:** Команда `clio create infrastructure` для развертывания PostgreSQL, Redis, pgAdmin в Kubernetes

**Текущее состояние:** ❌ Не существует такой команды  
**Что есть в deploy-creatio:** Ничего  
**Риск:** 🔴 Высокий - без инфраструктуры приложение не сможет работать

**Решение:**
```bash
# Требуется создать новую команду:
clio create infrastructure [--namespace creatio] [--storage-class default]

# Или встроить в create-dev-env:
clio create-dev-env --zip ... --setup-infra
```

---

### 2. **Системная настройка Maintainer (Фаза 2)**
**Требование:** Установка значения системной настройки `Maintainer` в Creatio

**Текущее состояние:** ❌ Не поддерживается  
**Что есть в deploy-creatio:** Ничего  
**Риск:** 🔴 Высокий - Maintainer требуется для нормальной работы системы

**Решение:**
```bash
# Новая команда или параметр:
clio set-syssetting --name Maintainer --value "John Doe"

# Или интегрировать в create-dev-env:
clio create-dev-env --zip ... --maintainer "John Doe"
```

---

### 3. **Конфигурация CookiesSameSiteMode (Фаза 2)**
**Требование:** Изменение значения `CookiesSameSiteMode` на `Lax` в `Terrasoft.WebHost.dll.config`

**Текущое состояние:** ❌ Не поддерживается  
**Что есть в deploy-creatio:** Ничего  
**Риск:** 🟡 Средний - влияет на безопасность cookies, требуется для правильной работы на macOS

**Решение:**
```csharp
// Добавить в Deploy Strategy:
private void UpdateWebHostConfig(DirectoryInfo appDir, PfInstallerOptions options)
{
    var configPath = Path.Combine(appDir.FullName, "Terrasoft.WebHost.dll.config");
    if (!File.Exists(configPath))
        return;
    
    var doc = new XmlDocument();
    doc.Load(configPath);
    
    var node = doc.SelectSingleNode("//CookiesSameSiteMode");
    if (node != null)
        node.InnerText = "Lax";
    
    doc.Save(configPath);
}
```

---

### 4. **CDP режим и File Design Mode (Фаза 1)**
**Требование:** Активировать `clio cdp true` и `clio fsm on` после развертывания

**Текущее состояние:** ⚠️ Команды существуют, но не встроены в deploy-creatio  
**Что есть в deploy-creatio:** Ничего  
**Риск:** 🟡 Средний - требуется для разработки, но может быть выполнено отдельно

**Решение:**
```csharp
// Встроить вызовы после регистрации окружения:
private void ActivateDevelopmentMode(PfInstallerOptions options)
{
    // clio cdp true
    ExecuteCommand("clio", "cdp true", options.EnvironmentName);
    
    // clio fsm on
    ExecuteCommand("clio", "fsm on", options.EnvironmentName);
}
```

---

## 🟡 Частичные пробелы (ТРЕБУЕТ РАСШИРЕНИЯ)

### 1. **Интерактивный ввод параметров**

**Требование из create-dev-env:**
- Environment Name (обязательно, спросить первым)
- Maintainer (обязательно, спросить вторым)
- Port (опционально, по умолчанию 8080)
- Credentials (опционально, по умолчанию Supervisor/Supervisor)

**Что есть в deploy-creatio:**
```
--SiteName           (не интерактивно)
--SitePort 80        (по умолчанию 80, не интерактивно)
--username Supervisor (не поддерживается)
--password Supervisor (не поддерживается)
```

**Решение:**
```csharp
[Option("env-name", HelpText = "Environment name")]
public string EnvironmentName { get; set; }

[Option("maintainer", HelpText = "System Maintainer setting")]
public string Maintainer { get; set; }

[Option("username", Default = "Supervisor", HelpText = "Database user")]
public string Username { get; set; }

[Option("password", Default = "Supervisor", HelpText = "Database password")]
public string Password { get; set; }

// Интерактивный ввод:
if (string.IsNullOrEmpty(options.EnvironmentName))
{
    _logger.WriteInfo("Enter Environment Name:");
    options.EnvironmentName = Console.ReadLine();
}

if (string.IsNullOrEmpty(options.Maintainer))
{
    _logger.WriteInfo("Enter Maintainer (optional):");
    options.Maintainer = Console.ReadLine();
}
```

---

### 2. **Папка environment**

**Требование:** Создавать папку с именем `environment` если `--target-dir` не указана

**Что есть в deploy-creatio:**
```
--app-path (опционально, по умолчанию ~/creatio/{SiteName})
```

**Решение:**
```csharp
// Если --target-dir не указана, создать environment папку в текущей директории
string targetDir = string.IsNullOrEmpty(options.TargetDir) 
    ? Path.Combine(Directory.GetCurrentDirectory(), "environment")
    : options.TargetDir;

Directory.CreateDirectory(targetDir);
```

---

## 🟢 Что уже поддерживается

| Возможность | Параметр | Статус |
|------------|----------|--------|
| Распаковка ZIP | --ZipFile | ✅ Полностью |
| Конфигурация подключения БД | --db pg\|mssql | ✅ Полностью |
| Обновление ConnectionString | Автоматически | ✅ Полностью |
| Выбор портов | --SitePort | ✅ Полностью |
| Кроссплатформенность | --deployment auto | ✅ Полностью (согласно plan) |
| Регистрация окружения | `clio cfg` | ✅ Может быть вызвана |

---

## 📋 Таблица фаз реализации

### Фаза 1: Минимум для работы на macOS

| # | Функция | Статус | Приоритет | Трудоемкость |
|---|---------|--------|-----------|-------------|
| 1.1 | Параметры Environment Name, Maintainer, Port, Credentials | ⚠️ Требует добавления | 🔴 Высокий | 🟢 Малая (2-4 часа) |
| 1.2 | Интерактивный ввод этих параметров | ❌ Отсутствует | 🔴 Высокий | 🟢 Малая (2-4 часа) |
| 1.3 | Папка environment по умолчанию | ⚠️ Требует настройки | 🟡 Средний | 🟢 Малая (1-2 часа) |
| 1.4 | Встроить `clio cdp true` после deploy | ⚠️ Требует встраивания | 🟡 Средний | 🟢 Малая (1-2 часа) |
| 1.5 | Встроить `clio fsm on` после deploy | ⚠️ Требует встраивания | 🟡 Средний | 🟢 Малая (1-2 часа) |

**Итого для Фазы 1:** ~9-14 часов разработки

---

### Фаза 2: Критически важные функции

| # | Функция | Статус | Приоритет | Трудоемкость |
|---|---------|--------|-----------|-------------|
| 2.1 | Команда `clio create infrastructure` | ❌ Отсутствует | 🔴 Высокий | 🔴 Высокая (8-12 часов) |
| 2.2 | Развертывание K8s (PostgreSQL, Redis, pgAdmin) | ❌ Отсутствует | 🔴 Высокий | 🔴 Высокая (12-16 часов) |
| 2.3 | Установка системной настройки Maintainer | ❌ Отсутствует | 🔴 Высокий | 🟡 Средняя (4-6 часов) |
| 2.4 | Конфигурация CookiesSameSiteMode | ❌ Отсутствует | 🟡 Средний | 🟢 Малая (2-3 часа) |
| 2.5 | Встроить `clio gate` в pipeline | ⚠️ Требует встраивания | 🟡 Средний | 🟢 Малая (2-3 часа) |

**Итого для Фазы 2:** ~28-40 часов разработки

---

### Фаза 3: Рефинировка и тестирование

| # | Функция | Статус | Приоритет | Трудоемкость |
|---|---------|--------|-----------|-------------|
| 3.1 | Unit тесты для новой команды | ⚠️ Требуется | 🟡 Средний | 🟡 Средняя (6-8 часов) |
| 3.2 | Integration тесты на macOS | ⚠️ Требуется | 🔴 Высокий | 🔴 Высокая (12-16 часов) |
| 3.3 | Документирование (Commands.md, примеры) | ⚠️ Требуется | 🟡 Средний | 🟢 Малая (4-5 часов) |
| 3.4 | Обработка ошибок и откат при сбое | ⚠️ Требуется | 🟡 Средний | 🟡 Средняя (6-8 часов) |

**Итого для Фазы 3:** ~28-37 часов разработки

---

## 💡 Вариант решения: Новая команда vs Расширение deploy-creatio

### Вариант A: Создать новую команду `create-dev-env` (Рекомендуется)

**Преимущества:**
- ✅ Более специфичная для macOS
- ✅ Может вызывать deploy-creatio внутри
- ✅ Не нарушает существующий функционал
- ✅ Лучше соответствует требованиям spec файла

**Недостатки:**
- ❌ Дублирование логики zip распаковки
- ❌ Две команды для одного процесса

**Структура:**
```
create-dev-env
├─ create infrastructure (K8s)
├─ deploy-creatio (приложение)
├─ set-syssetting Maintainer
├─ update-web-config (CookiesSameSiteMode)
├─ cdp true
├─ fsm on
└─ gate install (если нужна)
```

---

### Вариант B: Расширить deploy-creatio параметрами

**Преимущества:**
- ✅ Одна команда для всех платформ
- ✅ Нет дублирования кода
- ✅ Более гибко

**Недостатки:**
- ❌ Команда становится очень сложной
- ❌ На Windows не требуется создавать инфраструктуру

**Структура:**
```
deploy-creatio
├─ [опционально] create infrastructure
├─ unzip & copy files
├─ prepare database
├─ [опционально] set-syssetting
├─ [опционально] update-web-config
├─ deploy application
├─ [опционально] activate development modes
└─ register in clio
```

---

## 🎯 Рекомендация

**Создать новую команду `clio create-dev-env`**, которая будет:

1. **Обертка над deploy-creatio** с дополнительными шагами
2. **Специализирована для macOS** (хотя может работать и на других платформах)
3. **Выполнять последовательность операций** согласно требованиям spec
4. **Вызывать существующие команды** (gate, cfg, cdp, fsm, set-syssetting)
5. **Поддерживать интерактивный ввод** для Environment Name, Maintainer и других параметров

**Примерное время реализации:**
- Фаза 1 (базовая): 2-3 недели
- Фаза 2 (инфраструктура K8s): 3-4 недели
- Фаза 3 (тестирование, документация): 2 недели

**Общее время: 7-9 недель** при полной занятости одного разработчика

---

## 📝 Следующие шаги

1. **[СРОЧНО]** Согласовать архитектуру (новая команда vs расширение)
2. **[НЕДЕЛЯ 1]** Создать скелет новой команды с параметрами
3. **[НЕДЕЛЯ 2-3]** Реализовать интерактивный ввод параметров
4. **[НЕДЕЛЯ 4-6]** Реализовать инфраструктуру K8s
5. **[НЕДЕЛЯ 7-8]** Реализовать системные настройки (Maintainer, config)
6. **[НЕДЕЛЯ 9]** Тестирование и документирование

---

## 📚 Связанные документы

- [`create-dev-env-4-mac.md`](./create-dev-env-4-mac.md) - Требования
- [`deploy-creatio-cross-platform.md`](./deploy-creatio-cross-platform.md) - Текущие возможности deploy-creatio
- [`clio/Commands.md`](../clio/Commands.md) - Документация всех команд
