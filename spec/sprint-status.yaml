# Clio Sprint Status
# Managed by story-writer agent. Update Status manually or via agent.
#
# Statuses:
#   ready-for-dev  — story written, ADR approved, dev can pick it up
#   in-progress    — developer is actively working on it
#   review         — PR open, waiting for code review
#   done           — PR merged, story complete
#
# Sizes:
#   S — < 2 hours
#   M — half day
#   L — full day

stories: []

# Example entry (uncomment when first story is created):
#
#  - id: story-export-workspace-1
#    title: "Add --output flag to export-workspace command"
#    feature: export-workspace
#    size: M
#    status: ready-for-dev
#    file: spec/stories/story-export-workspace-1.md
#    prd: spec/prd/prd-export-workspace.md
#    adr: spec/adr/adr-export-workspace.md
