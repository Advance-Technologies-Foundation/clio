# Reuse native package bindings

Retain create-data-binding/add-data-binding-row and normal package compression/installation. Add the missing runtime rich-text mapping rather than a sequence serializer. Correct runtime 30 to LongText; runtime 43 is RichText. Stable package-owned row IDs define portable identity; external lookup dependencies must exist on the target, while users/mailboxes are configured there. Draft status prevents activation at install.
