# Compose existing tools

Use the actual MCP process and existing discovery, DataService batch, ESQ and native enrollment tools. Generate isolated record IDs for each run. Reuse an explicitly selected lab schedule and clone its rules to a dedicated high-capacity test ruleset. Retain fixtures for inspection. Compare three identical record updates as three single-item calls versus one three-item call; report calls/time/bytes, not inferred tokens or production throughput.
