я хочу найти все clio workspaces которые есть в файловой системы если я не указываю путь то должно найти на всех дисках и во всех папках или указать путь к папке в которой я хочу поискать без ограничения вложенности
для этого я хочу новую команду 
clio find-local-workspaces [optional] -d "{FOLDER_PATH}"
