clio create-k8-files
это генерирует нам файлы yaml в папку которая возле конфига clio
я не могу задать путь куда генерируются файлы
потом нужно звать несколько раз команды типа передавая туда путь который спрятан
kubectl apply -f infrastructure?
kubectl apply -f redis
в ходе команды clio dc --ZipFile build.zip не стартует .net сборка но открывается url
    возникла проблема база не поднялась потому что пользователь не смог подключится к серверу
    возниклда проблема что через pgadmin не разворачивался бекап писало нет места - побороли тем что переключили в настройках rancher Virtal Machine на virtiofs
хочется чтобы была команда для старта приложения типа clio start -e env_name которая запускает команду dotnet {EnvPath}/Terrasoft.WebHost.dll
