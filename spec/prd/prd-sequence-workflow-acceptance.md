# Sequence workflow acceptance

Issue #1580 integrates discovery, authoring, native enrollment and activity progression. Existing task tests own package generation and failure contracts. Add one opt-in end-to-end orchestration fixture, a measured same-workload batching comparison, guidance triggers and the curated guide-name pin. No real delivery or fabricated current activities.
