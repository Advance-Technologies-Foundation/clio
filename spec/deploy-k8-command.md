сейчас есть команда clio create-k8-files
это генерирует нам файлы yaml в папку  infrastructure которая возле конфига clio
я не могу задать путь куда генерируются файлы
потом нужно звать несколько раз команды для разворачивания инфраструктуры в k8 чтобы поднять необходимые сервисы типа передавая туда путь который спрятан
kubectl apply -f infrastructure/clio-namespace.yaml
kubectl apply -f infrastructure/clio-storage-class.yaml
kubectl apply -f infrastructure/redis
kubectl apply -f infrastructure/postgres
kubectl apply -f infrastructure/pgadmin

я хочу чтобы появилась команда clio deploy-infrastructure или clio di  которая выполнит все эти шаги с выводом в консоль шага и его результата чтобы пользователь мог видеть и в случае необходимости использовать лог вывода консоли для траблшутинга но при этом если все идет хоршо он видел прогресс и понимал на каком шаге/этапе находится и минимизировать необходимость дополнительного ввода после запуска команды deploy
считать команду выполненной успешно если удасться создать тестовое подключение к развернотому redis и postgres