secrets:
  values: 'YAML/Yaml-Samples/other_secrets.yaml'
  
  postgresql:
    password: pgPassword
  
  mssql:
    password: msPassword
    
  redis:
    passwords:
      passwordOne: passwordOneValue
      passwordTwo: passwordTwoValue