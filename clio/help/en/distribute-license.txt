NAME
    distribute-license - Add or remove users from a Creatio license package

SYNOPSIS
    clio distribute-license --package-id <PACKAGE> [--add-user <USER[,USER...]>] [--remove-user <USER[,USER...]>] [OPTIONS]

DESCRIPTION
    Calls LicenseManagerProxyService.svc/SaveLicenseData (the same endpoint
    used by the Supervisor "License" section in the Creatio UI) to grant or
    revoke a license package for one or more users. At least one of
    --add-user or --remove-user is required.

    --package-id, --add-user and --remove-user each accept either a Guid or
    an exact, case-insensitive display name. Names are resolved through the
    same license-exempt LicenseManagerProxyService.svc endpoints the
    Supervisor UI itself calls to populate its pickers (GetUsersList,
    GetLicenses), so name resolution works even on a fresh environment with
    zero distributed licenses. Resolution fails loudly (no request is sent)
    when a name matches zero or more than one record - pass the Guid
    directly in that case.

OPTIONS
    --package-id <PACKAGE>
        License package Id (Guid) or exact package name as shown in the
        Supervisor > License section in Creatio (required)

    --add-user <USER[,USER...]>
        User Id (Guid) or exact user name to add to the license package.
        Repeat the option or separate with ',' for multiple users.

    --remove-user <USER[,USER...]>
        User Id (Guid) or exact user name to remove from the license
        package. Repeat the option or separate with ',' for multiple users.

    --timeout <NUMBER>
        Request timeout in milliseconds (default: 100000)

    -e, --environment <ENVIRONMENT_NAME>
        Target environment name from your configuration

    Environment options (can be used instead of -e):
        -u, --uri <URI>
            Application URI

        -l, --Login <LOGIN>
            User login (administrator permission required)

        -p, --Password <PASSWORD>
            User password

EXAMPLES
    Grant a license package to a user by Guid:
        clio distribute-license --package-id 9c40e123-0a44-4cd2-94de-57341b8c3592 --add-user 7f3b869f-34f3-4f20-ab4d-7480a5fdf647 -e dev

    Revoke a license package from a user by Guid:
        clio distribute-license --package-id 9c40e123-0a44-4cd2-94de-57341b8c3592 --remove-user 7f3b869f-34f3-4f20-ab4d-7480a5fdf647 -e dev

    Grant a license package to multiple users at once:
        clio grant-license --package-id 9c40e123-0a44-4cd2-94de-57341b8c3592 --add-user id1,id2 -e dev

    Grant a license package by name instead of Guid:
        clio distribute-license --package-id "studio creatio on-site subscription" --add-user Supervisor -e dev

NOTES
    - Administrator permissions are required on the target environment.
    - --add-user and --remove-user may not both contain the same user Id.
    - A license package name or user name that resolves to zero or more than
      one record fails with an actionable error; pass the Guid directly to
      disambiguate.

SEE ALSO
    upload-licenses - Upload license files to Creatio
    upload-license - Load license to selected environment

REPORTING BUGS
    https://github.com/Advance-Technologies-Foundation/clio
