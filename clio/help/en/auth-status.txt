NAME
    auth-status - show cached SSO authentication status

ALIASES
    whoami

SYNOPSIS
    auth-status [options]

OPTIONS
    --Environment -e  Environment name

RETURN VALUES
    0  A session is cached. An expired access token is still reported as 0,
       because the next command renews it from the cached refresh token.
    1  No session is cached, or the environment cannot be resolved
