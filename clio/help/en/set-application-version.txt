NAME
    set-application-version - legacy heading for set-app-version

DESCRIPTION
    The Commands.md reference uses this heading for the set-app-version command.
    Use it to update application version metadata before packaging or publishing.

SYNOPSIS
    clio set-app-version [OPTIONS]

OPTIONS
    Supports the same options as the canonical set-app-version command.

EXAMPLES
    clio set-app-version --help
        Show the canonical command help and options

    clio set-app-version -e dev
        Run the canonical command against the dev environment when supported by the command contract

SEE ALSO
    set-app-version - Canonical command name in source code

REPORTING BUGS
    https://github.com/Advance-Technologies-Foundation/clio