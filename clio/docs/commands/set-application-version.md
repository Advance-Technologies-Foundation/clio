# set-application-version

Legacy heading for the application version update command.

## Synopsis

```bash
clio set-app-version [OPTIONS]
```

## Description

The `Commands.md` reference uses `set-application-version`, while the implementation uses
the `set-app-version` command name. Use this page as the canonical GitHub entry for that heading.

## Examples

```bash
clio set-app-version --help
```

## See also

- [Commands.md](../../Commands.md#set-application-version)
